<%= form_for(@over_profile) do |f| %>
  <% if @over_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@over_profile.errors.count, "error") %> prohibited this over_profile from being saved:</h2>

      <ul>
      <% @over_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
